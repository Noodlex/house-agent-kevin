{
  "config": {
    "step": {
      "user": {
        "title": "House Agent Kevin",
        "description": "Set your holiday window and a few settings. The entities Kevin controls are added afterwards from the card, right on the timeline.",
        "data": {
          "start_date": "Holiday start",
          "end_date": "Holiday end",
          "mode": "Planning mode",
          "rotation_length": "Rotation block length (days)",
          "jitter": "Swing (± minutes)",
          "safety_off": "Safety off time"
        }
      }
    },
    "abort": {
      "already_configured": "House Agent Kevin is already set up."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "House Agent Kevin — settings",
        "description": "Re-tune the plan settings. Entities and clips are edited from the card.",
        "data": {
          "start_date": "Holiday start",
          "end_date": "Holiday end",
          "mode": "Planning mode",
          "rotation_length": "Rotation block length (days)",
          "jitter": "Swing (± minutes)",
          "safety_off": "Safety off time"
        }
      }
    }
  }
}
